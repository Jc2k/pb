# FlashMoe Experiment Ledger

This file is for agents working on the pb FlashMoe backend. Keep it current whenever an experiment changes model quality, token latency, cache layout, Metal kernels, routing, expert I/O, tokenizer behavior, or `pb flashmoe` CLI behavior.

The goal is not to preserve every idea forever. The goal is to prevent accidental regressions into experiments that were already measured and found harmful, while keeping useful upstream `danveloper/flash-moe` lessons close to the pb implementation.

## Agent Rules

- Add or update a row whenever you try a FlashMoe optimization or correctness change.
- Record the command, prompt, token count, hardware-sensitive observation, and whether the output was sane.
- Keep discarded experiments in the table. A discarded row is a guardrail, not clutter.
- Prefer isolated math tests for kernels and routing before full-model smoke tests.
- If an experiment comes from upstream `danveloper/flash-moe`, say whether pb mirrors it exactly or deviates to support generic Qwen MoE / Qwen3-VL.
- Avoid hidden behavior toggled by environment variables. Prefer explicit CLI flags only when users need them, otherwise keep a single measured path.

## Upstream Reference

Source: https://github.com/danveloper/flash-moe

Upstream target:

- Model: Qwen3.5-397B-A17B.
- Hardware reference: M3 Max MacBook Pro, 48 GB unified memory, Apple SSD.
- Production quality target: 4-bit experts, not 2-bit, because 2-bit was faster but broke JSON/tool calling quality.
- Reported best 4-bit path: about 4.36 tok/s with an FMA dequant kernel.
- Architecture: 60 transformer layers, 45 GatedDeltaNet linear-attention layers, 15 full-attention layers, 512 experts, K=4 active experts per token, plus a shared expert, hidden size 4096.
- Expert storage: about 209 GB at 4-bit; each active expert is about 6.75 MB; each token reads K=4 experts per layer.

Concrete reference checks from the upstream source:

- Upstream `metal_infer/infer.m` reports TTFT separately, then reports `Generation` tok/s as `(total_generated - 1) / (total_time - ttft_ms)`. Compare pb against decode/generation token rows, not prefill-inclusive model wall time.
- Upstream `repack_experts.py` writes fixed per-layer files where each expert is a 7,077,888-byte block: gate weight/scales/biases, up weight/scales/biases, then down weight/scales/biases. Scale/bias payloads are BF16 bytes.
- Upstream expert GPU execution reads selected experts into one Metal buffer per expert and binds gate/up/down tensors by fixed offsets. pb keeps a more generic PBQ4 metadata wrapper for arbitrary Qwen MoE and Qwen3-VL support, but future performance work should move toward offset-bound whole-expert buffers or direct Metal read buffers to reduce component-level copying and buffer churn.

Upstream design principles to mirror when possible:

| Principle | Upstream lesson | pb guidance |
| --- | --- | --- |
| Trust the OS page cache | Custom Metal/malloc/LZ4 expert caches lost to memory pressure or overhead. | Do not add a custom expert cache unless a benchmark beats OS page cache on sustained generation and preserves quality. |
| Stream active experts only | Parallel `pread` of K active experts per layer is the intended I/O shape. | Keep expert I/O scoped to active routed experts; avoid broad speculative reads. |
| 4-bit is production | 2-bit achieved higher tok/s but damaged JSON/tool calling. | Treat Q4 as the quality-preserving baseline. Any lower-bit path must pass structured output tests. |
| FMA dequant matters | Rearranging dequant matvec to `fma(nibble, scale*x, bias*x)` was a kept speedup. | Preserve FMA-style Q4 math and parity tests against synthetic reference data. |
| Accelerate for linear attention | BLAS delta-net was materially faster than scalar CPU recurrence upstream. | Prefer Accelerate/Metal/structured kernels over scalar Rust for hot linear-attention loops. |
| Defer GPU expert work | Submit expert forward without waiting so CPU can prepare the next layer. | Keep deferred expert phases unless a measured simplification beats them. |
| Do not overlap SSD DMA with bandwidth-saturated GPU work casually | Upstream found unified memory contention canceled prefetch gains. | Treat prefetch/overlap experiments as suspicious until measured with per-bucket timings. |

## Upstream Experiments To Preserve

| Status | Experiment | Result | Keep-out / keep-in guidance |
| --- | --- | --- | --- |
| Keep | Q4 FMA dequant kernel | Improved sustained Q4 from about 3.90 to 4.36 tok/s upstream. | Keep FMA-style nibble dequant math covered by unit tests. |
| Keep | Trust OS page cache | Removing custom caches was foundational upstream. | Avoid Metal LRU, malloc expert caches, and hidden cache toggles. |
| Keep | GPU combine + norm in deferred expert command | Removed a CPU round trip in the upstream pipeline. | Do not split combine/norm back to CPU without timing evidence. |
| Keep | BLAS delta-net | Upstream reports large attention recurrence improvement over scalar code. | Keep recurrence math testable and avoid scalar-only rewrites in hot paths. |
| Keep | C/native tokenizer startup path | Upstream optimized tokenizer startup heavily. | Keep tokenizer loading measured; avoid slow startup regressions. |
| Discard | LZ4 expert compression | Full pipeline regressed despite smaller disk reads. | Do not compress experts for the main Q4 path without a fresh sustained benchmark. |
| Discard | F_RDADVISE / prefetch during GPU work | I/O improved but GPU waits regressed from unified memory contention. | Treat prefetch as a risky experiment; document any retry carefully. |
| Discard | Temporal/MLP expert prediction | Hit rates were too low for K=4; misses waste SSD bandwidth. | Avoid speculative routing reads unless all-expert-hit probability is addressed. |
| Discard | GPU LUT dequant | Indirect register access serialized and regressed upstream. | Do not replace FMA dequant with LUT without a focused kernel benchmark. |
| Discard | GPU private buffer compression | Shared-to-private blits cost more than matvec savings. | Avoid extra blit stages for expert payloads in the main path. |
| Discard | Spin-poll GPU wait | CPU contention hurt GPU throughput. | Keep blocking/wait behavior boring unless measured otherwise. |
| Discard | `dispatch_io` expert I/O | Dispatch data overhead was far slower than `pread`. | Prefer direct `pread`/FileExt style reads for expert payloads. |
| Discard | mmap expert files | Cold page faults were disastrous for 7 MB expert reads. | Do not mmap expert packs for cold streaming. |
| Discard | MTP speculative decoding | MoE verification still needs per-token expert I/O. | Do not assume dense-model speculative decode economics apply. |

## pb Current Baseline

Last verified: 2026-07-08 on local Apple Silicon release build.

Required smoke:

```bash
target/aarch64-apple-darwin/release/pb flashmoe infer --raw --max-tokens 1 --top-k 1 --temperature 0 "2+2="
```

Observed:

- Exit code: 0.
- Output: `sp` on a cleanly rebuilt `flashmoe-v2-mlxq4` cache.
- Backend load: about 0.5 s.
- One generated token: about 10.7 s.
- Quality: corrupt for the smoke prompt; the current default MLX Q4 backend must not be treated as working.
- Performance: not meaningful until first-token quality is fixed; the longer corrupt run decoded at about 0.47 tok/s.

Previous explicit BF16 cache runs could print `4`, but the current production target is the upstream-style
`hf://mlx-community/Qwen3.5-397B-A17B-4bit` runtime. Do not use the older BF16 result as evidence that the
default FlashMoe backend is correct.

Validation commands that passed for the current cleanup commit:

```bash
cargo fmt
cargo clippy --all-targets -- -D warnings
cargo test --all-targets
deno task build:web
deno task test:web
cargo build --release --target aarch64-apple-darwin
```

## pb Experiment Log

| Date | Status | Area | Experiment / change | Evidence | Follow-up |
| --- | --- | --- | --- | --- | --- |
| 2026-07-07 | Keep | CLI | Added `pb flashmoe infer` and `pb flashmoe bench` so the backend can be tested without the agent framework. | Release smoke `2+2=` exited 0 and printed `4`. | Keep the smoke command in AGENTS.md. |
| 2026-07-07 | Keep | Cache | Canonical cache version was `flashmoe-v1-densebf16`; old automatic migrations were removed. | Local cache worked with the smoke command; tests passed. | Superseded by the MLX 4-bit target below; do not add implicit migrations while this is single-user/local-cache only. |
| 2026-07-07 | Keep | Quality | Routing unit test matches upstream softmax-then-topK behavior. | `routing_weights_match_flashmoe_softmax_then_topk_reference` passes. | Extend parity tests before changing routing. |
| 2026-07-07 | Keep | Q4 math | BF16 scale/bias expert pack test mirrors upstream uint32/nibble packing expectations. | `q4_bf16_expert_pack_matches_flashmoe_uint32_nibble_reference` passes. | Preserve when touching pack format or Metal Q4 kernels. |
| 2026-07-07 | Keep | Dense runtime | Existing dense-Q4 reader/math tests remain, but post-hoc dense-Q4 cache writing from BF16/F32 tensors is disabled. | Focused FlashMoe tests and full `cargo test --all-targets` pass. | Re-enable post-hoc quantization only with measured benefit and cache-format docs. Native upstream-format Q4 import is tracked separately below. |
| 2026-07-07 | Discard | Config surface | FlashMoe-specific environment toggles were removed. | `rg PB_FLASHMOE src/inference/flashmoe/mod.rs src/lib.rs` returned no matches after cleanup. | Prefer deterministic behavior and explicit CLI flags. |
| 2026-07-07 | Discard | Migration | Old cache migration helpers/subcommand were removed. | `rg migrat src/inference/flashmoe/mod.rs src/lib.rs` returned no matches after cleanup. | Rebuild the local cache instead of carrying compatibility code unless users expand beyond one local cache. |
| 2026-07-07 | Discard | Routing | GPU/Metal route topK env-gated path is disabled; CPU routing is the stable path. | Smoke output sane; prior GPU routing experiments were not faster enough and added risk. | Retry only with isolated parity tests and timing evidence. |
| 2026-07-07 | Discard | Attention policy | Short-context Metal attention benchmarking path was removed; policy is fixed and simple. | Tests/clippy pass; upstream results note short-context GPU attention can be slower. | Revisit only with long-context benchmark rows. |
| 2026-07-07 | Open | Performance | Current pb backend is correct enough for smoke but far slower than upstream. | 1 token in about 9 s vs upstream 4-bit target above 4 tok/s. | Profile per-bucket timing from `pb flashmoe bench`; first suspect is dense projection / expert I/O pipeline shape. |
| 2026-07-07 | Keep | Refactor | Moved the historical FlashMoe monolith behind `src/inference/flashmoe/legacy.rs` and kept `mod.rs` as the stable facade. | `cargo fmt`, `cargo clippy --all-targets -- -D warnings`, and `cargo test --all-targets` passed after the move. | Continue extracting modules behind the facade without changing cache/runtime behavior. |
| 2026-07-07 | Keep | Refactor | Extracted public FlashMoe request/output/timing constants and structs to `types.rs`. | Same per-step fmt, clippy, and full Rust test suite passed. | Keep public API types out of execution/platform modules. |
| 2026-07-07 | Keep | Refactor | Extracted pure routing/Q4 math helpers to `math.rs`. | Same per-step fmt, clippy, and full Rust test suite passed. | Move more pure math only with parity tests nearby. |
| 2026-07-07 | Keep | Refactor | Added `inference::backend::InferenceBackend` so llama.cpp and FlashMoe can share text/chat/vision request and output shapes. | Same per-step fmt, clippy, and full Rust test suite passed. | Gradually migrate agent/CLI call sites onto the shared interface after behavior tests cover token accounting and tool-call parsing. |
| 2026-07-07 | Discard | Expert parsing | Tried moving each expert read buffer into shared parsed records so packed/raw scale-bias payload bytes were borrowed instead of copied. | Baseline `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 2 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-baseline.tsv`: output `4是`, decode row 2262.641 ms, expert_io 392.775 ms, expert_read 1103.986 ms, expert_compute 55.500 ms. Experiment `/private/tmp/pb-flashmoe-shared-pack-move.tsv`: output `4是`, decode row regressed to 2309.050 ms even though expert_io dropped to 248.866 ms and expert_read to 697.569 ms; expert_compute rose to 236.139 ms, likely from allocation/retention pressure around deferred Metal expert phases. Code reverted. | Do not retry shared backing buffers unless the allocator/retention side is redesigned, e.g. reusable owned expert buffers or direct Metal upload, and benchmark both expert I/O and total decode wall time. |
| 2026-07-07 | Keep | Metal dense matvec | Replaced per-dispatch scalar Metal buffers for dense mmap matvec parameters with `setBytes:length:atIndex:` so input/output buffers are the only temporary Metal buffers. This mirrors upstream's bias toward simpler command setup and avoids tiny allocation churn without changing routing, expert I/O, or model math. | Baseline `/private/tmp/pb-flashmoe-baseline.tsv` from `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 2 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-baseline.tsv`: output `4是`, model 11615.972 ms, decode row 2262.641 ms, attention_projection 622.069 ms. Experiment `/private/tmp/pb-flashmoe-dense-setbytes.tsv`: output `4是`, model 11434.461 ms, decode 2205.926 ms, attention_projection 615.578 ms. Repeat `/private/tmp/pb-flashmoe-dense-setbytes-2.tsv`: output `4是`, model 10713.548 ms, decode 2043.682 ms, attention_projection 617.310 ms. | Keep, but treat as an incremental setup cleanup rather than the main bottleneck fix. Next suspects remain expert I/O scheduling and the dense projection pipeline shape; compare steady decode rows, not prefill-inclusive model tok/s. |
| 2026-07-07 | Keep | LM-head sampling | For deterministic, unpenalized sampling on resident dense mmap weights, compute LM-head logits into a GPU buffer and run the existing `topk_vocab` kernel before reading back only the top candidates. This keeps the BF16 resident mmap matvec math unchanged and removes full-vocab CPU readback/top-k from the hot `top_k=1` benchmark path. | Prior kept setBytes run `/private/tmp/pb-flashmoe-dense-setbytes-2.tsv`: output `4是`, model 10713.548 ms, decode 2043.682 ms, sampling 188.649 ms. Experiment `/private/tmp/pb-flashmoe-resident-lmhead-topk.tsv`: output `4是`, model 10586.388 ms, decode 1775.777 ms, sampling 36.255 ms. Repeat `/private/tmp/pb-flashmoe-resident-lmhead-topk-2.tsv`: output `4是`, model 10675.003 ms, decode 1876.723 ms, sampling 42.274 ms. Trimmed final source `/private/tmp/pb-flashmoe-resident-lmhead-topk-trimmed.tsv`: output `4是`, model 10643.710 ms, decode 1734.607 ms, sampling 39.278 ms. | Keep. This improves deterministic decode but does not solve the 4 tok/s target; attention projections remain about 618-622 ms and expert I/O/read noise remains a major source of wall time. For stochastic sampling with repeat penalty, keep the existing CPU-adjusted candidate path unless a GPU penalty-aware top-k is added and tested. |
| 2026-07-07 | Discard | Expert parsing | Tried skipping eager BF16 scale/bias decode for expert packs in release builds, leaving raw BF16 bytes for the deferred Metal expert phase and decoding only for CPU fallback. A focused synthetic unit test passed before reverting, but the full model did not improve. | Prior kept LM-head topK trimmed run `/private/tmp/pb-flashmoe-resident-lmhead-topk-trimmed.tsv`: output `4是`, model 10643.710 ms, decode 1734.607 ms, expert_io 293.041 ms, expert_read 723.255 ms, expert_compute 47.978 ms. Experiment `/private/tmp/pb-flashmoe-lazy-bf16-scale-bias.tsv` from `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 2 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-lazy-bf16-scale-bias.tsv`: output `4是`, model 10680.982 ms, decode 1791.973 ms, expert_io 298.361 ms, expert_read 749.804 ms, expert_compute 49.445 ms, sampling 42.313 ms. Code reverted. | Do not retry lazy release-only BF16 decode in the current packed-record shape; parse decode is not the limiting wall-clock cost. If revisiting scale/bias handling, test direct Metal upload or reusable expert buffers against total decode wall time, not just parser allocation counts. |
| 2026-07-07 | Discard | Dense cache | Tried re-enabling dense-Q4 cache writing for attention projection matrices with a bumped local cache version (`flashmoe-v2-denseq4`), leaving `lm_head`, embeddings, router, norms, vision tensors, and shared experts unquantized. Dense-Q4 cache build required regenerating `model_weights.bin` and reused unchanged packed experts via local symlink for the benchmark. | Prior kept LM-head topK trimmed run `/private/tmp/pb-flashmoe-resident-lmhead-topk-trimmed.tsv`: output `4是`, model 10643.710 ms, decode 1734.607 ms, attention_projection 617.687 ms, attention_input_projection 184.356 ms, attention_output_projection 364.279 ms, sampling 39.278 ms. Experiment `/private/tmp/pb-flashmoe-denseq4-attn-projections.tsv` from `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 2 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-denseq4-attn-projections.tsv`: output `5`, model 12996.110 ms, decode 1421.127 ms, attention_projection 628.068 ms, attention_input_projection 188.597 ms, attention_output_projection 366.672 ms, sampling 39.290 ms. Initial broader attempt that also quantized shared experts failed before generation with `shared expert tensors for layer 0 do not match config`. Code reverted. | Do not revive dense-Q4 cache writing in this form: it did not reduce the projection bucket and it broke the deterministic smoke answer. If dense quantization is revisited, start with isolated quality/parity checks and a faster Metal Q4 dense kernel microbenchmark before changing the production cache version. |
| 2026-07-07 | Discard | Dense cache | Tried a closer upstream-shaped dense-Q4 variant for attention projection matrices: group size 64 with BF16 scale/bias payloads (`dense-q4-affine-bf16-v4`) and a matching mmap Metal kernel, while leaving `lm_head`, embeddings, router, norms, vision tensors, shared experts, and expert packs unquantized. The new dense store was generated in `flashmoe-v2-denseq4bf16` and unchanged experts were reused via the complete local `flashmoe-v1/packed_experts` symlink. | Baseline `/private/tmp/pb-flashmoe-current-baseline.tsv`: output `4是`, model 10096.616 ms, decode 1771.381 ms, attention_projection 545.161 ms, attention_input_projection 181.023 ms, attention_output_projection 303.208 ms, expert_io 343.553 ms, expert_read 1054.812 ms, expert_compute 34.120 ms, sampling 35.152 ms. Experiment `/private/tmp/pb-flashmoe-denseq4-bf16-attn.tsv` from `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 2 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-denseq4-bf16-attn.tsv`: exit code 0, output `5`, model 11990.175 ms, decode 1468.245 ms, attention_projection 633.335 ms, attention_input_projection 189.148 ms, attention_output_projection 367.302 ms, expert_io 222.809 ms, expert_read 630.550 ms, expert_compute 48.077 ms, sampling 39.158 ms. Code reverted. | Do not retry attention-projection dense-Q4 just by matching upstream group-64/BF16 scale-bias layout. It still breaks the deterministic smoke answer and worsens the projection bucket; any future dense quantization needs isolated numeric quality tests and a kernel microbenchmark that proves projection latency before a full cache-version change. |
| 2026-07-07 | Discard | Metal dense matvec | Tried computing two BF16 dense output rows per SIMD group in `dense_mmap_matvec_bf16_simd` and dispatching 16 rows/threadgroup so the 4096-float input cache is reused across more rows. | Prior kept LM-head topK trimmed run `/private/tmp/pb-flashmoe-resident-lmhead-topk-trimmed.tsv`: output `4是`, model 10643.710 ms, decode 1734.607 ms, attention_projection 617.687 ms, attention_input_projection 184.356 ms, attention_output_projection 364.279 ms, routing 19.802 ms, expert_io 293.041 ms, expert_read 723.255 ms, expert_compute 47.978 ms, sampling 39.278 ms. Experiment `/private/tmp/pb-flashmoe-bf16-two-rows-per-simd.tsv` from `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 2 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-bf16-two-rows-per-simd.tsv`: output `4是`, model 17858.003 ms, decode 1980.922 ms, attention_projection 649.432 ms, attention_input_projection 217.599 ms, attention_output_projection 364.931 ms, routing 15.272 ms, expert_io 366.184 ms, expert_read 1083.220 ms, expert_compute 47.281 ms, sampling 39.281 ms. Code reverted. | Do not increase rows per SIMD/threadgroup for this BF16 kernel; it worsened input projection and total decode, likely reducing useful parallelism/occupancy and not helping output projection. |
| 2026-07-07 | Discard | Metal dense matvec | Tried raising `dense_mmap_matvec_bf16_simd` threadgroup input cache from 4096 to 8192 floats so 8192-wide attention output projections could reuse the input vector instead of rereading it per row. The Metal compiler accepted the larger 32 KiB threadgroup array. | Rebuilt baseline `/private/tmp/pb-flashmoe-rebuilt-baseline.tsv` after reverting the dense-Q4 BF16 cache experiment: output `4是`, model 17882.418 ms, decode 1957.864 ms, attention_projection 617.423 ms, attention_input_projection 184.581 ms, attention_output_projection 365.070 ms, expert_io 369.603 ms, expert_read 1093.536 ms, expert_compute 51.496 ms, sampling 42.306 ms. Experiment `/private/tmp/pb-flashmoe-bf16-cache8192.tsv`: output `4是`, model 10971.615 ms, decode 1892.000 ms, attention_projection 618.310 ms, attention_input_projection 183.634 ms, attention_output_projection 365.288 ms, expert_io 369.002 ms, expert_read 1091.842 ms, expert_compute 43.953 ms, sampling 39.264 ms. Code reverted because the targeted projection buckets did not improve; model/decode movement was run noise. | Do not retry only enlarging the BF16 input cache. Output projection is still about 6 ms/layer, so the next dense-kernel attempt needs a different execution shape or the upstream prequantized dense-weight path, not just a bigger threadgroup cache. |
| 2026-07-07 | Discard | Expert Metal encoding | Tried mirroring upstream's scalar `setBytes` style for the q4 expert matvec dimension parameters inside pb's deferred expert phase, replacing four tiny Metal buffers per q4 matvec with inline bytes. This did not address pb's larger difference from upstream: pb still splits PBQ4 expert packs into component buffers instead of binding a single full expert buffer by offsets. | Fresh baseline `/private/tmp/pb-flashmoe-current-baseline.tsv` from `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 2 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-current-baseline.tsv`: output `4是`, model 10096.616 ms, decode 1771.381 ms, attention_projection 545.161 ms, expert_io 343.553 ms, expert_read 1054.812 ms, expert_compute 34.120 ms, sampling 35.152 ms. Experiment `/private/tmp/pb-flashmoe-q4-setbytes.tsv`: output `4是`, model 10829.483 ms, decode 1719.450 ms, attention_projection 615.603 ms, expert_io 299.666 ms, expert_read 730.871 ms, expert_compute 35.248 ms, sampling 39.262 ms. Repeat `/private/tmp/pb-flashmoe-q4-setbytes-repeat.tsv`: output `4是`, model 10451.088 ms, decode 1783.005 ms, attention_projection 614.729 ms, expert_io 298.634 ms, expert_read 715.161 ms, expert_compute 45.800 ms, sampling 39.267 ms. Code reverted. | Do not retry q4 scalar `setBytes` alone; any apparent decode improvement was read/cache noise, while the directly targeted expert compute bucket worsened. Next upstream-aligned work should compare whole-expert offset-bound Metal buffers and direct pread into reusable Metal buffers against total decode wall time. |
| 2026-07-07 | Discard | Expert Metal encoding | Tried a PBQ4 source-backed whole-pack path closer to upstream's one-Metal-buffer-per-expert design: read each expert into one retained buffer, parse component byte ranges, and bind gate/up/down packed/scale/bias slices with Metal buffer offsets when alignment permitted. This preserved pb's generic PBQ4 names/metadata instead of assuming upstream's fixed offsets. | Fresh baseline `/private/tmp/pb-flashmoe-current-baseline.tsv` from `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 2 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-current-baseline.tsv`: output `4是`, model 10096.616 ms, decode 1771.381 ms, attention_projection 545.161 ms, expert_io 343.553 ms, expert_read 1054.812 ms, expert_compute 34.120 ms, sampling 35.152 ms. Experiment `/private/tmp/pb-flashmoe-source-backed-pack.tsv` from the same command shape: output `4是`, model 11701.841 ms, decode 2099.891 ms, attention_projection 619.794 ms, expert_io 280.950 ms, expert_read 1013.789 ms, expert_compute 211.675 ms, sampling 51.358 ms. Code reverted. | Do not retry source-backed whole-pack offset binding in this retained-`Vec` form; it reduced no-copy intent but made the q4 expert compute bucket much worse. If revisiting upstream's buffer shape, pread directly into reusable `newBufferWithBytesNoCopy`/Metal-owned expert slots so the buffer base and offsets match the shader's efficient path, and benchmark expert compute plus total decode. |
| 2026-07-07 | Keep | Expert parsing | Added a metadata-ordered PBQ4 parser fast path for runtime expert reads. This mirrors upstream's fixed-layout assumption where safe, while preserving pb's generic PBQ4 format and current owned component buffers so the prior source-backed Metal regression is not reintroduced. | Current short baseline `/private/tmp/pb-flashmoe-fast-parser-baseline.tsv`: output `4是`, decode 1984.205 ms, attention_projection 614.274 ms, expert_io 429.326 ms, expert_read 1044.750 ms, expert_compute 51.987 ms, sampling 42.269 ms. Fast parser `/private/tmp/pb-flashmoe-fast-parser.tsv`: output `4是`, decode 1935.213 ms, attention_projection 622.836 ms, expert_io 370.479 ms, expert_read 1083.687 ms, expert_compute 50.927 ms, sampling 39.286 ms. Sustained fast parser `/private/tmp/pb-flashmoe-fast-parser-12.tsv`: output `<think>  </think>  Rust is a reddish-b`, decode_rows=11, avg_total_ms=1959.240, tok_s=0.510, attention_projection=617.591, expert_io=374.721, expert_read=1098.639, expert_compute=48.640, sampling=40.359. Prior sustained baseline `/private/tmp/pb-flashmoe-sustained-baseline-12.tsv`: decode_rows=11, avg_total_ms=1978.773, tok_s=0.505, attention_projection=618.267, expert_io=376.404, expert_read=1098.982, expert_compute=47.286, sampling=40.354. Focused parser parity tests pass. | Keep as a small cleanup only; the gain is about 1% sustained decode and does not address the 4 tok/s gap. The remaining large difference is still upstream's fully fixed expert layout / prequantized dense path versus pb's generic cache and BF16 dense projections. |
| 2026-07-07 | Keep | Dense cache | Added native MLX-Q4 dense import for upstream-style safetensors triples: `*.weight` as packed U32 plus BF16 `*.scales` and `*.biases`. The runtime store now writes those bytes directly as packed/scales/biases and the mmap Q4 path supports BF16 scale/bias payloads, while non-native BF16/F32 dense tensors remain unquantized. This mirrors upstream `extract_weights.py` native 4-bit dense storage without reviving the failed post-hoc attention-projection quantization. | Synthetic `dense_manifest_imports_native_mlx_q4_triples` builds a U32/BF16/BF16 safetensors fixture, verifies aux tensors are skipped from the manifest, verifies logical shape expansion from `[1,1]` to `[1,8]`, verifies runtime bytes are copied as `[packed][scales][biases]`, decodes BF16 scale/bias rows, and projects to `22.0`. Focused tests `cargo test dense_manifest_imports_native_mlx_q4_triples --all-targets`, `cargo test dense_manifest_preserves_non_native_dense_weights --all-targets`, and `cargo test metal_shader_source_defines_full_forward_kernel_set --all-targets` pass. Full gates `cargo fmt`, `cargo clippy --all-targets -- -D warnings`, `cargo test --all-targets`, `deno task build:web`, `deno task test:web`, and `cargo build --release --target aarch64-apple-darwin` pass. Release smoke `target/aarch64-apple-darwin/release/pb flashmoe infer --raw --max-tokens 1 --top-k 1 --temperature 0 "2+2="` exited 0, generated 1 token in 9290 ms, and printed `4`. | Keep as an enabling cache-format fix, not a measured speedup on the current local BF16 snapshot. Full-model speed should be remeasured only with a native U32/scales/biases dense snapshot; the remaining 4 tok/s gap still requires upstream-format dense weights and likely whole-expert buffer/offset work. |
| 2026-07-07 | Keep | Metal dense matvec | Resident dense mmap projections now dispatch all rows for supported resident dtypes (`F32`/`BF16`) instead of reusing the decoded-tile 64 MiB heuristic. This removes the unnecessary two-way split for BF16 `o_proj` `[4096,8192]` while preserving the old cap for decoded/raw fallback paths. | Baseline after native-Q4 import `/private/tmp/pb-flashmoe-current-after-native-q4.tsv`: output `4是命题吗？_百度知道 2+2=`, decode_rows=11, avg_total_ms=1897.376, tok_s=0.527, attention_projection=618.371, attention_input_projection=184.589, attention_kernel=68.451, attention_output_projection=364.784, expert_io=354.377, expert_read=1089.889, expert_compute=45.337, sampling=40.637. Experiment `/private/tmp/pb-flashmoe-resident-fullrow.tsv`: output `4是命题吗？_百度知道 2+2=`, decode_rows=11, avg_total_ms=1622.385, tok_s=0.616, attention_projection=431.963, attention_input_projection=184.819, attention_kernel=68.365, attention_output_projection=178.261, expert_io=342.406, expert_read=1004.090, expert_compute=41.778, sampling=42.151. Focused test `resident_dense_mmap_projection_uses_full_row_dispatch` passes. | Keep. This is a real BF16-cache speedup and keeps the same model output, but still leaves pb far below 4 tok/s. Next dense work should target input projection and true upstream native-Q4 dense weights; avoid reintroducing decoded-tile limits on resident mmap paths. |
| 2026-07-07 | Discard | Routing | Tried computing router scores through the resident Metal dense mmap path for unquantized router weights, instead of the current CPU/Accelerate cached dense rows. This superficially mirrors upstream's GPU-heavy command flow, but pb dispatches routing as a separate per-layer matvec and pays command-buffer/wait overhead that upstream avoids by fusing routing into the broader forward pipeline. | Prior kept BF16 resident full-row baseline `/private/tmp/pb-flashmoe-resident-fullrow.tsv`: output `4是命题吗？_百度知道 2+2=`, decode_rows=11, avg_total_ms=1622.385, tok_s=0.616, attention_projection=431.963, routing about 20 ms/token, expert_io=342.406, expert_read=1004.090, expert_compute=41.778, sampling=42.151. Experiment `/private/tmp/pb-flashmoe-resident-router-metal.tsv` from `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 12 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-resident-router-metal.tsv`: output `4是命题吗？_百度知道 2+2=`, decode_rows=11, avg_total_ms=1718.241, tok_s=0.582, attention_projection=386.301, routing=152.964, expert_io=340.518, expert_read=1060.513, expert_compute=35.494, sampling=41.930. Code reverted. | Do not route through a standalone resident Metal matvec per layer. Retry only as part of a fused GPU command path that keeps router scoring, topK, residual/norm, and downstream expert work in one scheduled sequence, with parity tests for routing weights and decode-only timing. |
| 2026-07-07 | Discard | Cache selection | Tried switching the canonical cache version from `flashmoe-v1-densebf16` back to the complete older `flashmoe-v1` cache, which has a smaller dense store and some quantized attention projection entries. This was a concrete upstream-shaped dense-Q4 check against the current runtime, but the cache is still numerically wrong for the deterministic smoke prompt. | Temporary one-line `CACHE_VERSION = "flashmoe-v1"` release build. Smoke `target/aarch64-apple-darwin/release/pb flashmoe infer --raw --max-tokens 1 --top-k 1 --temperature 0 "2+2="` exited 0 but generated 1 token in 13068 ms and printed `5`. The current BF16 cache smoke prints `4` and the latest sustained decode baseline remains `/private/tmp/pb-flashmoe-resident-fullrow.tsv` at decode_rows=11, avg_total_ms=1622.385, tok_s=0.616. Code reverted. | Do not select `flashmoe-v1` as canonical just because its dense file is smaller. Native upstream dense-Q4 support must come from a quality-preserving U32/scales/biases source or isolated numeric fixes, not this stale local cache. |
| 2026-07-07 | Discard | Linear attention | Tried replacing the two Accelerate SGEMV reads in the GatedDeltaNet recurrence with one skinny SGEMM that computes `state * key` and `state * query` together, then derives the post-update output as `pre_update_query_projection + delta * dot(key, query)`. The math tests passed and aggregate token wall time looked better, but layer-kind timing showed the targeted linear-attention kernel was slower; the apparent token speedup came from expert I/O noise. | Baseline `/private/tmp/pb-flashmoe-resident-fullrow.tsv`: output `4是命题吗？_百度知道 2+2=`, decode_rows=11, avg_total_ms=1622.385, tok_s=0.616; layer decode averages: linear_attention total=13.053 ms/layer, kernel=0.498 ms/layer, expert_io=5.690 ms/layer. Experiment `/private/tmp/pb-flashmoe-gated-delta-sgemm.tsv`: output `4是命题吗？_百度知道 2+2=`, decode_rows=11, avg_total_ms=1549.626, tok_s=0.645, attention_kernel=67.155. Repeat `/private/tmp/pb-flashmoe-gated-delta-sgemm-repeat.tsv`: decode_rows=11, avg_total_ms=1481.804, tok_s=0.675, attention_kernel=74.827. Combined layer decode averages across both experiment files: linear_attention total=12.605 ms/layer, kernel=0.583 ms/layer, expert_io=5.350 ms/layer. Code reverted. | Do not replace the current two-SGEMV recurrence with skinny SGEMM. Future linear-attention work should compare against upstream's exact BLAS data layout or move more of the recurrence into a fused GPU path; judge by linear_attention kernel/layer timing, not token total alone. |
| 2026-07-07 | Keep | Timing | Added explicit `deferred_wait_ms` timing to token/layer TSV rows and charged the wait for a deferred expert command back to the layer that submitted it. This mirrors upstream's `deferred_wait` bucket and makes the CMD3 overlap question visible instead of hiding waits between layer rows. | Fresh release benchmark `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 12 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-deferred-wait-timing.tsv`: output `4是命题吗？_百度知道 2+2=`, decode_rows=11, avg_total_ms=1591.349, tok_s=0.628, attention_projection=434.375, routing=15.150, deferred_wait=753.835, expert_io=297.753, expert_read=754.801, warm_read=295.177, expert_compute=46.453, sampling=42.225. Layer decode averages showed linear_attention deferred_wait=12.862 ms/layer and full_attention deferred_wait=11.669 ms/layer. `cargo fmt`, `cargo clippy --all-targets -- -D warnings`, `cargo test --all-targets`, and release build passed. | Keep the column. The next optimization should focus on why pb's deferred expert command still leaves about 754 ms/token of wait, while upstream's pipeline targets CMD3 defer plus GPU combine/norm feeding the next layer. Compare token rows for tok/s, but use layer `deferred_wait_ms` to judge CMD3 overlap experiments. |
| 2026-07-07 | Keep | Metal RMS norm | Added a reduced one-threadgroup Metal RMS norm kernel for the deferred expert next-layer norm path. The old generic kernel launched one thread per output element and each thread rescanned the full width, so this mirrors upstream's reduced RMS norm shape for the hottest deferred path without changing the existing generic kernel for other call sites. | Baseline `/private/tmp/pb-flashmoe-deferred-wait-timing.tsv`: output `4是命题吗？_百度知道 2+2=`, decode_rows=11, avg_total_ms=1591.349, tok_s=0.628, deferred_wait=753.835. Experiment `/private/tmp/pb-flashmoe-reduced-rmsnorm.tsv`: output same, decode_rows=11, avg_total_ms=1631.057, tok_s=0.613, deferred_wait=744.242, expert_io=346.440, expert_read=957.000. Repeat `/private/tmp/pb-flashmoe-reduced-rmsnorm-repeat.tsv`: output same, decode_rows=11, avg_total_ms=1571.919, tok_s=0.636, deferred_wait=732.554, expert_io=300.503, expert_read=755.182. Focused shader test, `cargo fmt`, `cargo clippy --all-targets -- -D warnings`, `cargo test --all-targets`, and release build passed. | Keep as a targeted reduction in deferred norm work, but treat total-token movement as noisy because expert read/I/O still dominates. Next CMD3 work should keep the reduced norm and focus on avoiding the remaining deferred wait or keeping outputs resident into the following command sequence. |
| 2026-07-07 | Keep | GPU/CPU boundary | When a deferred expert phase already produced the next layer's input norm in a Metal buffer, enqueue the next layer's resident dense input projections directly from that buffer before waiting for the deferred phase's CPU readback. This mirrors upstream's CMD3-to-CMD1 queue dependency: the next command can consume the previous command's GPU output, and the CPU only waits when it needs projection results / residual hidden state. | Fresh baseline after reduced RMS norm `/private/tmp/pb-flashmoe-current-rms-baseline.tsv`: output `4是命题吗？_百度知道 2+2=`, decode_rows=11, avg_total_ms=1624.283, tok_s=0.616, attention_projection=436.069, input=184.377, deferred_wait=740.879, expert_io=340.818, expert_read=942.106, expert_compute=45.517, sampling=40.025. Experiment `/private/tmp/pb-flashmoe-deferred-input-proj.tsv`: same output, decode_rows=11, avg_total_ms=1038.496, tok_s=0.963, attention_projection=678.220, input=423.390, deferred_wait=12.648, expert_io=255.971, expert_read=717.933, expert_compute=35.101, sampling=43.631. Repeat `/private/tmp/pb-flashmoe-deferred-input-proj-repeat.tsv`: same output, decode_rows=11, avg_total_ms=973.947, tok_s=1.027, attention_projection=623.806, input=372.724, deferred_wait=13.196, expert_io=249.989, expert_read=710.965, expert_compute=34.492, sampling=40.824. `cargo fmt`, `cargo clippy --all-targets -- -D warnings`, `cargo test --all-targets`, and release build passed. | Keep. This is the first major upstream-aligned queueing fix and proves pb's explicit layer-boundary wait was a real bug. Attention input projection now carries the GPU queue wait, so future work must judge total token time and drill into residual CPU readback / expert I/O rather than re-adding an explicit deferred wait. Next target: keep more residual/combine state resident or move router/post-attention norm into a fused GPU sequence. |
| 2026-07-07 | Keep | Cache compatibility | Gated the deferred Metal input-projection shortcut by the active dense tensor format so unsupported resident dense caches fall back to the old CPU-visible hidden-state path instead of failing. This keeps the upstream-like CMD3-to-CMD1 fast path for the canonical BF16 cache while allowing stale/local Q4 caches to run far enough for quality checks. | Before the gate, a temporary model root that mapped `flashmoe-v1-densebf16` to the older local `flashmoe-v1` cache failed with exit code 1 and `deferred Metal linear-attention input projection is unavailable`. After the gate, `target/aarch64-apple-darwin/release/pb flashmoe infer --model-dir /private/tmp/pb-flashmoe-cache-check --raw --max-tokens 1 --top-k 1 --temperature 0 "2+2="` exited 0 and printed `5`, confirming compatibility fallback works but that cache is still quality-broken. Canonical BF16 benchmark `/private/tmp/pb-flashmoe-deferred-input-gated.tsv`: output `4是命题吗？_百度知道 2+2=`, decode_rows=11, avg_total_ms=966.008, tok_s=1.035, attention_projection=605.954, input=351.601, deferred_wait=12.416, expert_io=251.836, expert_read=713.379, expert_compute=33.861, sampling=42.754. Focused shader test and release build passed before full gates. | Keep as a compatibility guard, not a speed feature. Do not select stale Q4 caches until the deterministic smoke prints `4`; future native-Q4 work should add isolated numeric parity before enabling the deferred-buffer shortcut for quantized dense tensors. |
| 2026-07-07 | Keep | Measurement | Added explicit prefill/TTFT-vs-decode throughput reporting to `pb flashmoe infer` and `pb flashmoe bench`. Upstream `metal_infer/infer.m` reports TTFT separately and computes generation tok/s from tokens after the first sampled token, so pb now prints a directly comparable `decode_tok_s` line while keeping the existing TSV rows for deeper analysis. | Release bench `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 4 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-throughput-report.tsv`: output `4是命题吗`, generated 4 tokens in 10498 ms, printed `prefill_or_ttft_ms=6920 decode_tokens=3 decode_ms=3576 decode_tok_s=0.839`. TSV parse: decode_rows=3, avg_total_ms=1192.073, tok_s=0.839, attention_projection=805.965, input=543.367, deferred_wait=18.311, expert_io=268.924, expert_read=755.471, expert_compute=37.057, sampling=36.268. Focused unit test `flashmoe_throughput_summary_reports_decode_only_tok_s` passed. | Keep. This does not improve raw speed, but it prevents prefill-inclusive wall time from being compared to upstream's generation-only headline. A one-token smoke proves correctness only; use at least two generated tokens, preferably sustained decode rows, for tok/s claims. |
| 2026-07-07 | Keep | MLX 4-bit import | Fixed ingestion for the actual upstream-style `mlx-community/Qwen3.5-397B-A17B-4bit` snapshot by canonicalizing its `language_model.*` tensor prefix and recognizing `model.layers.*.mlp.switch_mlp.{gate,up,down}_proj.weight` aggregate expert tensors. This lets pb's native MLX-Q4 dense import and PBQ4 expert packer see the same U32/BF16 source layout that upstream uses, instead of silently treating those names as unsupported dense tensors or missing experts. | Hugging Face HEAD probe confirmed `model.safetensors.index.json` exists for commit `39159bd8aa74f5c8446d2b2dc584f62bb51cb0d3`; sampled shard header `/private/tmp/qwen35-mlx-model-00001.header.part` showed dense and aggregate expert weights as `U32` with BF16 `.scales`/`.biases`, e.g. `language_model.model.layers.0.linear_attn.in_proj_qkv.weight` shape `[12288,512]` and `language_model.model.layers.0.mlp.switch_mlp.gate_proj.weight` shape `[512,1024,512]`. Focused tests `cargo test mlx --all-targets`, `cargo test qwen35_hf_tensor_names_are_canonicalized_for_runtime --all-targets`, and `cargo test expert_tensor_classifier_ignores_mtp_speculative_layers --all-targets` pass. | Keep as a necessary upstream-format cache fix. It is not a measured tok/s improvement until the full 224 GB MLX 4-bit snapshot is pulled and rebuilt; next verification should run `pb pull hf://mlx-community/Qwen3.5-397B-A17B-4bit`, then the deterministic `2+2=` smoke and sustained decode benchmark against that cache. |
| 2026-07-07 | Keep | Cache selection | Made `hf://mlx-community/Qwen3.5-397B-A17B-4bit` the canonical Qwen3.5 FlashMoe model and bumped the runtime cache version to `flashmoe-v2-mlxq4`. The original BF16 source remains addressable explicitly as `hf://Qwen/Qwen3.5-397B-A17B`, but default `pb flashmoe` and legacy Qwen3-Coder aliases now point at the same 4-bit snapshot as upstream. | Tests updated so `canonical_model(DEFAULT_MODEL)` resolves to `mlx-community/Qwen3.5-397B-A17B-4bit`, while `canonical_model(QWEN35_BF16_MODEL)` remains the BF16 URI. | Keep. This is selection plumbing, not a measured speedup. Next step is still to pull/build the full MLX cache and run smoke plus sustained decode timing. |
| 2026-07-07 | Keep | MLX 4-bit experts | Native MLX-Q4 aggregate expert tensors now carry their U32 weight plus BF16 `.scales`/`.biases` source metadata through the manifest and PBQ4 packer. The packer expands logical shapes for validation, slices packed rows plus matching scale/bias groups, and writes PBQ4 records from those bytes directly instead of decoding U32 or requantizing. This mirrors upstream's prepacked 4-bit expert layout while keeping pb's generic PBQ4 metadata and decoded fallback path for non-native fixtures. | Focused tests `cargo test packer_copies_native_mlx_q4_switch_mlp_experts_without_requantizing --all-targets` and `cargo test packer_splits_mlx_switch_mlp_aggregate_expert_tensors --all-targets` pass. The synthetic MLX fixture asserts exact packed, scale, and bias byte slices for both expert 0 and expert 1, plus a Q4 projection sanity check that returns `22.0` rows from the copied bytes. Full gates `cargo fmt`, `cargo clippy --all-targets -- -D warnings`, `cargo test --all-targets`, and `cargo build --release --target aarch64-apple-darwin` pass. | Keep as an enabling upstream-format cache fix, not a measured tok/s win on the current cache. Do not reintroduce a U32 decode/requant path for native MLX experts; speed must be remeasured after rebuilding the full `mlx-community/Qwen3.5-397B-A17B-4bit` cache. |
| 2026-07-07 | Open | Cache availability | Default `pb flashmoe` now correctly targets the upstream MLX 4-bit snapshot, but the local `mlx-community_Qwen3.5-397B-A17B-4bit/flashmoe-v2-mlxq4` cache is absent and cannot be pulled with the current free space. A temporary model root can point the explicit BF16 model at the existing `flashmoe-v1-densebf16` runtime for continued correctness/perf measurements without mutating the user's cache. | Default smoke `target/aarch64-apple-darwin/release/pb flashmoe infer --raw --max-tokens 1 --top-k 1 --temperature 0 "2+2="` failed before inference with missing MLX cache artifacts. Disk check showed `/Users/john/.local/share/pb/models` uses about 1.0 TiB and the volume has about 66 GiB free, below the upstream 224 GB MLX snapshot. Temporary BF16 smoke via `/private/tmp/pb-flashmoe-bf16-model` exited 0 and printed `4`. Temporary BF16 sustained bench `/private/tmp/pb-flashmoe-after-native-expert-q4.tsv` printed `decode_tokens=11 decode_ms=11480 decode_tok_s=0.958`; TSV parse: `avg_total_ms=1043.638`, `attention_projection=677.553`, `input=424.055`, `expert_io=258.341`, `expert_read=721.437`, `sampling=41.377`. | Keep goal open. The next true upstream-format verification needs either freeing/repointing enough disk for the MLX 4-bit cache or changing cache preparation to avoid requiring both downloaded safetensors and duplicated runtime artifacts at once. Do not compare the 0.958 tok/s BF16 baseline to upstream's 4-bit headline as if the cache formats are equivalent. |
| 2026-07-08 | Keep | Cache selection | Made FlashMoe cache-version selection model-specific: the canonical default `mlx-community/Qwen3.5-397B-A17B-4bit` still uses `flashmoe-v2-mlxq4`, while the explicit BF16 source URI `hf://Qwen/Qwen3.5-397B-A17B` uses the existing `flashmoe-v1-densebf16` runtime. This restores honest BF16 benchmarking without a temporary symlink while keeping the upstream MLX 4-bit cache as the default target. | Focused tests `cargo test explicit_bf16_qwen35_uses_bf16_cache_layout --all-targets`, `cargo test default_model_canonicalizes_to_flashmoe_safetensors_snapshot --all-targets`, and `cargo test plan_uses_flashmoe_cache_layout --all-targets` pass. Release smoke `target/aarch64-apple-darwin/release/pb flashmoe infer --model hf://Qwen/Qwen3.5-397B-A17B --raw --max-tokens 1 --top-k 1 --temperature 0 "2+2="` exited 0, loaded `/Users/john/.local/share/pb/models`, generated 1 token in 6849 ms, and printed `4`. Default MLX Q4 smoke still fails before inference with missing `mlx-community_Qwen3.5-397B-A17B-4bit/flashmoe-v2-mlxq4` artifacts, as expected. | Keep as a measurement/cache unblocker, not a speedup. Continue treating the MLX 4-bit cache as the production target; use explicit BF16 only for fallback correctness and bottleneck experiments while disk space blocks the true upstream-format run. |
| 2026-07-08 | Discard | Expert Metal encoding | Tried mirroring upstream `metal_infer/infer.m` more closely by splitting pb's deferred expert command buffer into a shared-expert encoder, two encoders per active expert (`gate+up`, then `SwiGLU+down`), and a final combine/norm encoder. This preserved pb's existing Q4 kernels and buffer layout, avoiding the bad fused-down variant that would recompute SiLU for every output row. | Current BF16 baseline `target/aarch64-apple-darwin/release/pb flashmoe bench --model hf://Qwen/Qwen3.5-397B-A17B --raw --prompt "2+2=" --max-tokens 12 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-real-bf16-baseline.tsv`: output `4是命题吗？_百度知道 2+2=`, `decode_rows=11`, `avg_total_ms=958.085`, `tok_s=1.044`, `attention=589.135`, `input=334.434`, `expert_io=264.277`, `expert_read=789.187`, `expert_compute=32.366`. Experiment `/private/tmp/pb-flashmoe-split-encoders.tsv`: same output, printed `decode_tokens=11 decode_ms=11852 decode_tok_s=0.928`; TSV parse `decode_rows=11`, `avg_total_ms=1077.463`, `tok_s=0.928`, `attention=687.399`, `input=430.566`, `expert_io=283.309`, `expert_read=853.945`, `expert_compute=28.657`. Code reverted. | Do not retry encoder splitting alone. It slightly improves the narrow expert-compute bucket but regresses sustained decode wall time and shifts wait into the attention/input path. Future upstream-parity work should target data layout or real queue dependencies, not just adding command encoders around the same copied component buffers. |
| 2026-07-08 | Open | Cache availability | Audited local disk after the MLX Q4 cache miss. The true upstream-format run is blocked by space, not by planner selection: `/Users/john/.local/share/pb/models/Qwen_Qwen3.5-397B-A17B` is about 1.0 TiB, mostly old BF16 source shards and stale experimental runtime directories, while the working explicit BF16 runtime is `flashmoe-v1-densebf16`. | `du -sh /Users/john/.local/share/pb/models/*` showed `Qwen_Qwen3.5-397B-A17B` at 1.0 TiB and `unsloth_Qwen3-Coder-Next-GGUF` at 45 GiB. Inside the Qwen directory, stale runtime dirs include `flashmoe-v1` (212 GiB), `flashmoe-v2` (14 GiB), `flashmoe-v2-denseq4` (12 GiB), `flashmoe-v2-denseq4bf16` (9 GiB), and `flashmoe-v3` (12 GiB), plus 94 BF16 `model.safetensors-*` shards of about 8-9 GiB each. A deletion attempt was rejected pending explicit user approval, as it should be. | With explicit approval, delete only stale BF16 source shards and stale experimental runtime dirs, keeping `flashmoe-v1-densebf16`, then pull/build `hf://mlx-community/Qwen3.5-397B-A17B-4bit` and rerun the default `2+2=` smoke plus sustained decode benchmark. Do not keep tuning BF16-only paths as a substitute for the required MLX Q4 verification. |
| 2026-07-08 | Keep | Cache cleanup | Added `pb flashmoe cache-clean` as a dry-run-first cleanup command for stale FlashMoe cache artifacts. It plans from the active model cache version, preserves the planner's active runtime directory, excludes source safetensor shards unless `--source-shards` is passed, and requires `--yes` before deleting anything. | Synthetic unit tests create BF16 cache directories with stale `flashmoe-*` runtimes, active `flashmoe-v1-densebf16`, source shards, and unrelated config files; they verify dry runs do not delete, stale runtime deletion preserves the active runtime, and source shard deletion is opt-in. Real dry run `target/debug/pb flashmoe cache-clean --source-shards` preserved `/Users/john/.local/share/pb/models/Qwen_Qwen3.5-397B-A17B/flashmoe-v1-densebf16` and listed 99 candidates totaling 1010.20 GiB. | Keep as an operational unblocker for the MLX Q4 run, not as a speed optimization. Use a dry run first, then an explicit `--yes --source-shards` invocation only when intentionally freeing local BF16 source shards after confirming the active BF16 runtime is preserved. |
| 2026-07-08 | Open | Performance baseline | Reran the explicit BF16 cache after the cleanup command work to avoid optimizing against stale numbers. This is still not the upstream-format target because dense tensors are BF16, while upstream's 4 tok/s result is measured on the MLX 4-bit cache and reports decode throughput separate from TTFT. | `target/aarch64-apple-darwin/release/pb flashmoe bench --model hf://Qwen/Qwen3.5-397B-A17B --raw --prompt "2+2=" --max-tokens 12 --temperature 0 --top-k 1 --status kept --results /private/tmp/pb-flashmoe-current-before-next.tsv` exited 0, output `4是命题吗？_百度知道 2+2=`, and printed `decode_tokens=11 decode_ms=10435 decode_tok_s=1.054`. TSV parse: `decode_rows=11`, `avg_total_ms=948.693`, `attention_projection=602.833`, `attention_input_projection=350.585`, `attention_output_projection=183.551`, `expert_io=246.855`, `expert_read=714.329`, `expert_compute=34.756`, `sampling=37.836`. A guarded `target/debug/pb flashmoe cache-clean --source-shards --yes` attempt was rejected pending explicit user approval for deleting those exact local artifacts. | Do not spend another iteration chasing BF16 dense projection speed as a substitute for the required MLX Q4 run. The next aligned step is explicit approval to run `pb flashmoe cache-clean --source-shards --yes`, then pull/build the default `hf://mlx-community/Qwen3.5-397B-A17B-4bit` cache and benchmark default decode throughput. |
| 2026-07-08 | Keep | Pull cache hygiene | Added `pb pull --flashmoe-prune-source-shards` so FlashMoe Hugging Face pulls can delete downloaded `model.safetensors-*` source shards after the runtime cache has been built successfully. This does not delete stale runtime directories and does not run on failed builds, so resume/debug data is preserved unless the runtime is ready. | Focused synthetic test `source_shard_cleanup_does_not_delete_runtime_dirs` verifies source shard pruning deletes only source shards while preserving both the active runtime and a stale `flashmoe-*` runtime directory. | Keep as a future peak-disk hygiene feature for MLX Q4 pulls. This still does not solve the current local free-space blocker by itself, because the MLX Q4 runtime needs substantial space before source pruning can happen. |
| 2026-07-08 | Discard | MLX Q4 runtime | Built the full local `hf://mlx-community/Qwen3.5-397B-A17B-4bit` `flashmoe-v2-mlxq4` runtime after adding source-shard pruning and native Q4 dense/runtime readers. The cache now loads and exits successfully, but the model is numerically wrong from the first sampled token, so these runtime changes must not be treated as a performance improvement. | Correctness smoke `target/aarch64-apple-darwin/release/pb flashmoe infer --raw --max-tokens 1 --top-k 1 --temperature 0 "2+2="` exited 0, generated 1 token in 8105 ms, and printed `sp` instead of a plausible `4` continuation. Longer smoke `target/aarch64-apple-darwin/release/pb flashmoe infer --raw --max-tokens 8 --top-k 1 --temperature 0 "2+2="` exited 0 with `decode_tok_s=0.484` and output `sp imagin,>\n-ingdocument Prom`. Discarded timing run `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "2+2=" --max-tokens 4 --temperature 0 --top-k 1 --status discarded --results /private/tmp/pb-flashmoe-mlxq4-corrupt.tsv` printed output `sp imagin,>` and `decode_tok_s=0.504`; token totals showed attention projection around 615 ms, routing around 190 ms, deferred wait hundreds of ms, and expert I/O hundreds of ms. A later clean rebuild after freeing disk removed the old `flashmoe-v2-mlxq4` runtime, pulled with `--flashmoe-prune-source-shards --parallel 1`, pruned all 46 source shards (`208.49 GiB`), and produced the same bad result: one-token smoke printed `sp` in 10731 ms, while an eight-token smoke printed `sp imagin,>\n-ingdocument Prom` with `decode_tok_s=0.469`. Upstream `metal_infer/infer.m` reports generation tok/s after TTFT and uses K=4 for speed, so this is not only a prefill-accounting mismatch or stale-cache artifact. | Do not commit or optimize the current MLX Q4 runtime path until first-token quality is fixed. The next retry should isolate native Q4 CPU-vs-Metal parity on actual mmap offsets/side arrays and compare pb's PBQ4 record parsing/copying against upstream `repack_experts.py` fixed expert blocks. Keep using decode-only tok/s for fair upstream comparisons, but require the deterministic `2+2=` smoke to print coherent English/math before claiming speed progress. |
| 2026-07-08 | Discard | MLX Q4 diagnostics | After disk space was freed, restored enough MLX source shards to verify real source/runtime bytes and ran targeted execution bypasses. These checks rule out several tempting false leads without producing a quality fix. | Source byte checks against `model-00001-of-00046.safetensors` and `model-00046-of-00046.safetensors` showed exact runtime matches for layer-0 `linear_attn.in_proj_qkv`, `model.embed_tokens`, layer-0 expert 0 gate/up/down packed/scales/biases, and `lm_head.weight`. Temporarily disabling the dense Q4 mmap batch path still printed `sp` (`generated 1 token in 7073 ms`). Temporarily forcing CPU expert execution still printed `sp` (`14309 ms`). Temporarily skipping shared experts changed the bad token to `P` (`6305 ms`) but did not restore a sane continuation. All diagnostic code was reverted. | Do not re-chase source offset/copy corruption, Q4 mmap batch alone, Metal expert encoding alone, or shared experts alone as the primary first-token bug. The next useful isolation should compare actual q4 matvec outputs with an upstream-compatible executable or add a small standalone Metal parity harness that does not use the crashing ignored test path. |
| 2026-07-08 | Keep | Qwen3.5 norm offsets | Made Qwen3Next RMSNorm offset application data-aware. The original BF16 Qwen3.5 source stores offset-style norm weights, e.g. layer-0 `input_layernorm.weight` mean about `-0.072` and final `model.norm.weight` mean about `0.670`, so pb must add `1.0`. The MLX 4-bit snapshot stores already-offset full norm weights, e.g. the same tensors mean about `0.928` and `1.670`, so adding `1.0` again corrupts logits. Upstream `infer.m` applies RMSNorm weights directly, which matches the MLX cache representation. | Focused test `cargo test qwen3next_norm --all-targets` passed, then full gates `cargo test --all-targets` and `cargo clippy --all-targets -- -D warnings` passed. Release smoke improved from gibberish to coherent first tokens: default K=4 `target/aarch64-apple-darwin/release/pb flashmoe infer --raw --max-tokens 1 --top-k 1 --temperature 0 "2+2="` exited 0 and printed `5` in 7723 ms instead of `sp`; with `--active-experts 6` it printed `4`; K sweep showed K=4/5/8 -> `5` and K=6/7/9/10 -> `4`. Longer K=6 raw math still degraded (`4，4+4=4，`, `decode_tok_s=0.460`), and raw English completion produced `4.   The.` with timing captured in `/private/tmp/pb-flashmoe-normfix-k6-decode.tsv` (`decode_tok_s=0.553`). | Keep the norm fix; it is required for MLX Q4 correctness and preserves BF16 offset-style weights. The backend is still not complete: default top-4 routing gives the wrong first token and decode after a correct first token drifts. Next work should isolate decode/KV or linear-attention/full-attention state parity after the first generated token, and should not claim the model is fixed from first-token-only K=6/K=10 results. |
| 2026-07-08 | Discard | Raw PBQ4 expert Metal buffers | Tried retaining the whole PBQ4 expert blob and teaching the deferred Metal expert phase to bind a single raw pack buffer plus per-record offsets for gate/up/down matvecs, closer to upstream's fixed-offset expert buffer dispatch. The fallback component-buffer path was preserved for unsafe offsets. This kept output quality unchanged but slowed sustained decode, likely because the extra whole-pack retention/copy pressure outweighed the reduced component-buffer setup in pb's current PBQ4 parser shape. Code was reverted. | Prior K=6 MLX Q4 norm-fix bench `/private/tmp/pb-flashmoe-normfix-k6-decode.tsv` printed `4.   The.` with `decode_tok_s=0.553`. Experiment `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "The answer to two plus two is" --max-tokens 6 --temperature 0 --top-k 1 --active-experts 6 --status discarded --results /private/tmp/pb-flashmoe-raw-pack-expert.tsv` exited 0, printed the same `4.   The.`, but regressed to `decode_tok_s=0.436`; model wall time rose to `26412 ms`, decode time to `11472 ms`, and per-token rows showed higher deferred waits/routing noise rather than a clear expert I/O win. | Do not retry merely retaining whole PBQ4 blobs while still parsing/copying records. A future upstream-shaped expert path should pread directly into reusable Metal buffers or cache-line/page-aligned slots and bind offsets without also keeping duplicated per-record vectors; benchmark total decode wall time, not only buffer setup. |
| 2026-07-08 | Discard | MLX Q4 K=10 fallback | Tested whether using the model-configured `num_experts_per_tok=10` would restore quality enough to justify deviating from upstream's K=4 speed shortcut. It fixed neither sustained decode quality nor speed. | `target/aarch64-apple-darwin/release/pb flashmoe infer --raw --max-tokens 12 --top-k 1 --temperature 0 --active-experts 10 "The answer to two plus two is"` exited 0, generated 12 tokens in `44100 ms`, printed `decode_tok_s=0.407`, and output `4.\n\n The.\n\nThe  sp1`. The first token is plausible, but subsequent tokens still drift into repetition/gibberish and throughput is worse than K=6. | Do not switch the default to K=10 as a quality fix. The issue is downstream state/logit drift after the first generated token, not only too few active experts. Keep comparing K choices as diagnostics, but prioritize KV/linear-attention/full-attention state parity and tokenizer/chat-template correctness. |
| 2026-07-08 | Discard | MLX Q4 routing | Tried routing Q4 router-score projection through pb's generic dense projection helper so MLX `model.layers.*.mlp.gate.weight` could use the resident Metal mmap Q4 matvec path instead of row-by-row CPU dequantization. This mirrors part of upstream `infer.m`'s fused routing direction, but in pb's current separated pipeline it did not improve end-to-end decode. Code was reverted. | Baseline `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "The answer to two plus two is" --max-tokens 4 --top-k 1 --temperature 0 --active-experts 4 --results /private/tmp/pb-flashmoe-current-profile.tsv --status discarded` generated 2 tokens, output `<think>`, and decoded at `0.580 tok/s` with two decode-token totals: `total_wall_ms=3448.269`, `routing_ms=384.019`, `deferred_wait_ms=1165.641`, `expert_read_ms=1061.727`, `sampling_ms=309.046`. Experiment `/private/tmp/pb-flashmoe-router-metal-profile.tsv` generated the same 2-token `<think>` output and decoded at `0.576 tok/s`; routing improved to `305.226 ms`, but total decode worsened to `3474.954 ms`, deferred wait rose to `1214.008 ms`, and expert read rose to `1218.577 ms`. | Do not route MLX Q4 router scores through the generic dense projection helper as an isolated change. Any future upstream-style routing work should fuse post-attention o-proj, residual, RMSNorm, router matvec, and shared-expert projections into one command phase like upstream, then benchmark total decode wall time and quality, not just the routing bucket. |
| 2026-07-08 | Keep | Tokenizer fallback | Added a structured-chat fallback for tokenizers that have Qwen `<|im_start|>`/`<|im_end|>` special tokens but no `chat_template`, matching the local `mlx-community/Qwen3.5-397B-A17B-4bit` tokenizer config. Explicit tokenizer templates still take precedence and invalid templates still error. | Before the change, non-raw `target/aarch64-apple-darwin/release/pb flashmoe infer --max-tokens 8 --top-k 1 --temperature 0 --active-experts 4 "What is 2+2? Answer in one sentence."` exited 1 with `tokenizer_config.json is missing chat_template`. After the change, the same command exited 0, generated 1 token in `35818 ms`, reported `decode_tok_s=0.514`, and printed `2`. Focused test `qwen_structured_renderer_falls_back_to_chatml_without_tokenizer_template` covers the fallback. | Keep as a CLI correctness fix for the default MLX cache, but do not count it as a model-quality or performance fix: the answer is still wrong and decode is still far below 4 tok/s. Continue isolating the numerical/runtime drift after template handling. |
| 2026-07-08 | Keep | Metal ownership | Fixed Objective-C ownership for FlashMoe Metal command buffers and compute encoders by retaining the autoreleased `commandBuffer` / `computeCommandEncoder` results before the existing `release` calls. The resident dense mmap BF16 and Q4 parity tests previously crashed with SIGSEGV after the test body returned, which made the resident fast path look suspect even though dispatch/readback had succeeded. | Before the change, `cargo test arm_macos_dense_bf16_mmap_simd_matches_cpu_reference_at_runtime_width --all-targets -- --ignored` and `cargo test arm_macos_dense_q4_mmap_matches_cpu_reference --all-targets -- --ignored` both exited with signal 11. After the change, both tests pass. This is a stability/parity-test fix, not a tok/s improvement; it preserves resident mmap buffers instead of disabling them. | Keep the retain/release balance for all Metal dispatch paths. Do not interpret the old ignored-test crash as evidence that resident dense mmap Q4 math is wrong; continue debugging MLX Q4 quality with numerical parity/state tests. |
| 2026-07-08 | Discard | Expert parsing | Retried lazy expert scale/bias decode on the current MLX Q4 cache after disk space was freed: runtime expert records kept BF16 scale/bias bytes for Metal and decoded f32 only on CPU fallback. The focused expert-pack tests and full Rust suite passed, but the full-model benchmark did not improve and output quality stayed wrong. Code was reverted. | Baseline after Metal ownership `/private/tmp/pb-flashmoe-current-k6-after-metalown.tsv`: output `4.   The.`, avg decode token wall `2163.877 ms`, `attention_projection=620.600`, `expert_io=303.231`, `expert_read=1061.432`, `expert_compute=53.384`, `sampling=187.592`. Lazy parse retry `/private/tmp/pb-flashmoe-current-k6-lazyparse.tsv`: same output `4.   The.`, avg decode token wall `2183.868 ms`, `attention_projection=622.544`, `expert_io=295.402`, `expert_read=1051.257`, `expert_compute=52.900`, `sampling=187.942`. | Do not reintroduce lazy BF16 scale/bias decode in the current PBQ4 record shape. It slightly reduces the narrow expert-I/O bucket but does not improve total decode or correctness; pursue state/logit parity or a direct reusable Metal-buffer expert path instead. |
| 2026-07-08 | Discard | Metal dense matvec | Tried mirroring upstream `dequant_matvec_4bit_v5` inside pb's BF16-scale/bias Q4 word paths by building a 16-entry per-group LUT and replacing per-nibble `float(code)` FMAs. Resident Q4 mmap parity still passed, but the full MLX Q4 decode benchmark did not improve the dense projection bucket and total decode slowed. Code was reverted. | Baseline after Metal ownership `/private/tmp/pb-flashmoe-current-k6-after-metalown.tsv`: output `4.   The.`, avg decode token wall `2163.877 ms`, `tok_s=0.462`, `attention_projection=620.600`, `attention_input_projection=188.614`, `attention_output_projection=366.430`, `deferred_wait=802.576`, `expert_io=303.231`, `sampling=187.592`. LUT experiment `/private/tmp/pb-flashmoe-current-k6-q4lut.tsv`: same output `4.   The.`, avg decode token wall `2261.788 ms`, `tok_s=0.442`, `attention_projection=620.132`, `attention_input_projection=188.108`, `attention_output_projection=366.306`, `deferred_wait=893.327`, `expert_io=305.413`, `sampling=188.530`. | Do not retry the LUT substitution alone. Upstream's v5 kernel is not the missing speedup when transplanted into pb's current mmap/batched projection path; the next dense experiment should compare command-buffer structure, batched projection boundaries, and GPU/CPU synchronization against upstream rather than only the inner dequant expression. |
| 2026-07-08 | Discard | Metal dense matvec | Tried replacing the resident dense-Q4 mmap scalar-parameter buffers with `setBytes` in the single and batched Q4 mmap helpers, mirroring the kept BF16 dense setup cleanup and upstream's scalar binding style. The focused resident Q4 parity test passed, but two full MLX Q4 decode runs were indistinguishable from noise and one repeat regressed total decode. Code was reverted. | Baseline after Metal ownership `/private/tmp/pb-flashmoe-current-k6-after-metalown.tsv`: output `4.   The.`, avg decode token wall `2163.877 ms`, `tok_s=0.462`, `attention_projection=620.600`, `attention_input_projection=188.614`, `attention_output_projection=366.430`, `deferred_wait=802.576`, `expert_io=303.231`, `expert_read=1061.432`, `sampling=187.592`. First setBytes run `/private/tmp/pb-flashmoe-current-k6-q4dense-setbytes.tsv`: same output, avg decode wall `2159.866 ms`, `tok_s=0.463`, `attention_projection=617.167`, `deferred_wait=810.832`, `expert_io=290.646`, `expert_read=1006.372`, `sampling=194.891`. Repeat `/private/tmp/pb-flashmoe-current-k6-q4dense-setbytes-repeat.tsv`: same output, avg decode wall `2183.600 ms`, `tok_s=0.458`, `attention_projection=616.205`, `deferred_wait=827.592`, `expert_io=303.992`, `expert_read=1041.763`, `sampling=187.182`. | Do not retry scalar `setBytes` for resident dense-Q4 alone. The dense projection bucket shifts by only a few milliseconds and total decode is dominated by queue/deferred waits, expert I/O/read variance, and still-broken MLX Q4 quality; next dense work should target actual command-buffer boundaries or state/logit parity. |
| 2026-07-08 | Keep | MLX Q4 diagnostics | Added explicit `pb flashmoe infer --trace-candidates` candidate logging. It prints the sampled top-k token ids, processed scores, and decoded one-token strings through the existing progress callback, with no default behavior or hot-path change when the flag is absent. This gives future agents a cheap logits-boundary check before trying another performance transplant from upstream. | Gates passed before the model run: `cargo fmt --check`, `cargo test --all-targets` (`363 passed, 5 ignored`), `cargo clippy --all-targets -- -D warnings`, and `cargo build --release --target aarch64-apple-darwin`. Default K=4 trace `target/aarch64-apple-darwin/release/pb flashmoe infer '2+2=' --raw --max-tokens 4 --temperature 0 --top-k 10 --trace-candidates` exited 0, printed `5`, and showed first-token candidates `5` score `11.828012`, `4` score `11.668925`; the next step ranked empty special token id `248046` first. K=6 trace `target/aarch64-apple-darwin/release/pb flashmoe infer '2+2=' --raw --max-tokens 4 --temperature 0 --top-k 10 --active-experts 6 --trace-candidates` exited 0, printed `4，4+`, and showed first-token `4` score `12.368827`, `5` score `12.314592`, then punctuation/CJK-heavy drift from the second token. | Keep the CLI diagnostic. This confirms the current MLX Q4 bug is visible in candidate logits immediately: default K=4 slightly prefers the wrong math token, and K=6 fixes the first token but not decode-state quality. Next correctness work should compare pb hidden/logit vectors against an upstream-compatible executable around prefill final logits and the first generated-token decode step before optimizing for tok/s. |
| 2026-07-08 | Keep | MLX Q4 diagnostics | Extended `--trace-candidates` to use the full-logits path only while tracing and print hidden/logit RMS, max absolute value, and finite flags, matching upstream `infer.m`'s debug anchors. The default optimized sampling path is unchanged when tracing is off. While comparing against upstream code, ruled out several false leads: split-half RoPE matches upstream, text KV cache positions match upstream per-layer append semantics, linear q/k normalization is equivalent because pb's helper is L2-based, and the stray `rsqrt(intermediate)` kernel is not used by the active Q4 expert MLP path. | Gates before release/model run: `cargo fmt --check`, `cargo test --all-targets` (`363 passed, 5 ignored`), `cargo clippy --all-targets -- -D warnings`, `git diff --check`, and `cargo build --release --target aarch64-apple-darwin`. K=4 trace `target/aarch64-apple-darwin/release/pb flashmoe infer '2+2=' --raw --max-tokens 2 --temperature 0 --top-k 10 --trace-candidates` exited 0, output `5`, first-token `hidden_rms=1.234709`, `hidden_max=25.126392`, `logits_rms=2.605496`, `logits_max=12.509108`, candidates `5` then `4`; second step `hidden_rms=1.297615`, `logits_rms=2.370760`, candidate id `248046` first. K=6 trace `target/aarch64-apple-darwin/release/pb flashmoe infer '2+2=' --raw --max-tokens 3 --temperature 0 --top-k 10 --active-experts 6 --trace-candidates` exited 0, output `4，4`, first-token `hidden_rms=1.208358`, `logits_rms=2.577525`, candidates `4` then `5`; after the punctuation token, hidden stats jumped to `hidden_rms=1.705379`, `hidden_max=73.802361`. | Keep. The new evidence points at decode-state drift after the first generated token rather than tokenizer decoding, candidate sorting, or basic LM-head finiteness. Next useful correctness work should snapshot or compare the hidden vector before/after the first generated-token forward pass, especially linear-attention recurrent state and deferred expert combine boundaries. |
| 2026-07-08 | Keep | MLX Q4 performance | Added Q4 dense projection from an existing Metal input buffer so the deferred expert phase's next-layer normed buffer can feed MLX-Q4 attention input projections. This mirrors upstream `fused_layer_forward`'s fast path where CMD3 from layer N and CMD1 for layer N+1 stay queued on Metal instead of forcing a CPU wait before Q/K/V or linear-attention input projections. The existing BF16 deferred-input path is unchanged. | Fresh baseline before the change `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "The answer to two plus two is" --max-tokens 6 --temperature 0 --top-k 1 --active-experts 4 --status discarded --results /private/tmp/pb-flashmoe-current-k4-fresh.tsv`: exited 0, output `<think>`, `decode_tok_s=0.578`, avg decode token wall `1730.737 ms`, `attention_projection=537.455`, `deferred_wait=585.213`, `expert_io=224.749`, `sampling=154.913`. Experiment `/private/tmp/pb-flashmoe-q4-deferred-input.tsv`: exited 0 with same output, `decode_tok_s=0.610`, avg decode wall `1638.744 ms`, `attention_projection=991.697`, `deferred_wait=20.194`, `expert_io=217.897`, `sampling=181.553`. Repeat `/private/tmp/pb-flashmoe-q4-deferred-input-repeat.tsv`: exited 0 with same output, `decode_tok_s=0.606`. K=6 smoke `target/aarch64-apple-darwin/release/pb flashmoe infer --raw --max-tokens 6 --temperature 0 --top-k 1 --active-experts 6 "The answer to two plus two is"` exited 0, printed `4.\n\n The.`, `decode_tok_s=0.576`; `2+2=` K=6 exited 0 and printed `4，4`. | Keep as a modest upstream-aligned overlap fix, not the main speed fix. It proves pb's previous MLX-Q4 path was missing the deferred-input fast path and was paying an avoidable wait, but total decode only improves about 5-6% because the wait collapses into slower Q4 input-buffer projection time. Next performance work should fuse the next phase like upstream CMD2: o_proj + residual + post-attn norm + router/shared expert projections in one Metal command buffer, rather than only moving the wait boundary. |
| 2026-07-08 | Discard | Metal dense matvec | Tried adding upstream `dequant_matvec_4bit_fast`-style resident dense-Q4 mmap kernels for inputs wider than 4096, with one output row per 64-thread threadgroup, strict packed-row/group/alignment guards, and both F32 and BF16 scale/bias variants. This targeted MLX Q4 `o_proj` shapes where upstream avoids the 4096-input cached v3 kernel. The focused shader-list test, ignored resident-Q4 Metal parity test, and release build passed, but the full-model speedup was below noise for a large diff. Code was reverted. | Prior kept baseline `/private/tmp/pb-flashmoe-q4-deferred-input-repeat.tsv`: output `<think>`, `decode_tok_s=0.606`, avg decode token wall `1650.715 ms`, `attention_projection=1009.137`, `attention_input_projection=586.029`, `attention_output_projection=359.517`, `deferred_wait=15.235`. Experiment `target/aarch64-apple-darwin/release/pb flashmoe bench --raw --prompt "The answer to two plus two is" --max-tokens 6 --temperature 0 --top-k 1 --active-experts 4 --status discarded --results /private/tmp/pb-flashmoe-q4-fastwide.tsv` exited 0, output `<think>`, `decode_tok_s=0.596`, avg decode wall `1678.892 ms`, `attention_output_projection=354.816`. Repeat `/private/tmp/pb-flashmoe-q4-fastwide-repeat.tsv` exited 0, same output, `decode_tok_s=0.621`, avg decode wall `1609.637 ms`, `attention_output_projection=355.203`. | Do not retry only transplanting upstream's wide single-row Q4 matvec into pb's current separated mmap path. It trims only about 4 ms/token from output projection and does not materially move total decode. The useful upstream difference is still the fused command-buffer phase around `o_proj` + residual + post-attn norm + router/shared expert projections, or correctness/state parity after the first generated token. |

## Required Regression Notes For Future Agents

When changing FlashMoe code, include these in the final response and update the table above:

- Exact command used for correctness smoke.
- Exit code and model output.
- Tokens generated and elapsed time.
- Any per-token/per-layer timing file path from `pb flashmoe bench`.
- Whether the result should be kept, discarded, or retried.
- If deviating from upstream `danveloper/flash-moe`, why pb needs to deviate for generic Qwen MoE or Qwen3-VL support.

## Source Links

- Upstream repository: https://github.com/danveloper/flash-moe
- Upstream Q4 optimization notes: https://github.com/danveloper/flash-moe/blob/main/docs/optimization-experiments-q4.md
- Upstream experiment TSV: https://github.com/danveloper/flash-moe/blob/main/results.tsv
