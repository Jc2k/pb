Create exactly two files and no others. Create alpha.txt containing exactly `alpha completion fixture` followed by a newline. Create beta.txt containing exactly `beta completion fixture` followed by a newline. Satisfy every trusted acceptance check, complete the fresh code review, and finish with the harness-owned semantic commit. Do not use commands or generated files to create the requested files; use the native file mutation tools.
