Build a dependency-free JavaScript slugify module. Create slugify.mjs exporting a named function `slugify(input)`. It must convert the input to lowercase, replace every run of characters outside ASCII a-z and 0-9 with one hyphen, and remove leading or trailing hyphens. Create slugify.test.mjs with meaningful Deno tests covering ordinary words, whitespace, punctuation, repeated separators, numbers, and empty input. Create README.md documenting the exact behavior and a short import example. Satisfy all three trusted acceptance checks, complete the fresh code review, and finish with the harness-owned semantic commit. Do not add dependencies or any other files.
